# Pino reservas API

API REST construida con **Node.js, Express y TypeScript** siguiendo buenas prácticas.

## 🚀 Scripts

- `npm run build` — compilar a `dist/`
- `npm run dev` — levantar el servidor en modo desarrollo con `tsx`
- `npm run format` — formatear código con Prettier
- `npm run lint:fix` — corregir errores automáticamente
- `npm run lint:staged` — correr ESLint y Prettier en archivos staged (lint-staged)
- `npm run lint` — correr ESLint (flat config)
- `npm run prepare` — instalar hooks de Husky
- `npm start` — ejecutar la versión compilada
- `npm test` — correr pruebas con Jest

## 🛠️ Tecnologías

- Node.js + Express
- TypeScript
- ESLint + Prettier + Husky + lint-staged
- Jest + Supertest (tests de integración)
- Winston (logging)
